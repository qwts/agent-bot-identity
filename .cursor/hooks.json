{
  "version": 1,
  "hooks": {
    "beforeShellExecution": [
      {
        "command": "node tools/agent-guard/guard-agent-command.mjs --protocol=cursor"
      },
      {
        "command": "H=\"${AGENT_BOT_HOOK_BIN:-$HOME/.local/share/agent-bot/agent-hook}\"; [ -x \"$H\" ] || exit 0; exec \"$H\" --dialect cursor --event pre-command # agent-bot agent-hook",
        "failClosed": true
      }
    ],
    "sessionStart": [
      {
        "command": "H=\"${AGENT_BOT_HOOK_BIN:-$HOME/.local/share/agent-bot/agent-hook}\"; [ -x \"$H\" ] || exit 0; exec \"$H\" --dialect cursor --event session-start # agent-bot agent-hook"
      }
    ],
    "sessionEnd": [
      {
        "command": "H=\"${AGENT_BOT_HOOK_BIN:-$HOME/.local/share/agent-bot/agent-hook}\"; [ -x \"$H\" ] || exit 0; exec \"$H\" --dialect cursor --event session-end # agent-bot agent-hook"
      }
    ],
    "beforeSubmitPrompt": [
      {
        "command": "H=\"${AGENT_BOT_HOOK_BIN:-$HOME/.local/share/agent-bot/agent-hook}\"; [ -x \"$H\" ] || exit 0; exec \"$H\" --dialect cursor --event prompt-submit # agent-bot agent-hook",
        "failClosed": true
      }
    ],
    "preToolUse": [
      {
        "command": "H=\"${AGENT_BOT_HOOK_BIN:-$HOME/.local/share/agent-bot/agent-hook}\"; [ -x \"$H\" ] || exit 0; exec \"$H\" --dialect cursor --event pre-tool-use # agent-bot agent-hook",
        "failClosed": true
      },
      {
        "command": "H=\"${AGENT_BOT_HOOK_BIN:-$HOME/.local/share/agent-bot/agent-hook}\"; [ -x \"$H\" ] || exit 0; exec \"$H\" --dialect cursor --event pre-file-write # agent-bot agent-hook",
        "failClosed": true
      }
    ],
    "postToolUse": [
      {
        "command": "H=\"${AGENT_BOT_HOOK_BIN:-$HOME/.local/share/agent-bot/agent-hook}\"; [ -x \"$H\" ] || exit 0; exec \"$H\" --dialect cursor --event post-tool-use # agent-bot agent-hook"
      }
    ],
    "stop": [
      {
        "command": "H=\"${AGENT_BOT_HOOK_BIN:-$HOME/.local/share/agent-bot/agent-hook}\"; [ -x \"$H\" ] || exit 0; exec \"$H\" --dialect cursor --event agent-stop # agent-bot agent-hook",
        "failClosed": true
      }
    ]
  }
}
