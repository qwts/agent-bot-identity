{
  "name": "agent-bot-identity",
  "version": "0.2.0",
  "description": "Per-harness GitHub App bot identities for coding agents — commits and PRs authored by the right bot for the IDE that made them, via git-native hooks, with fail-closed gh and transcript-bound Agent IDs.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "bin": {
    "agent-bot": "./agent-bot"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node sync-hooks.mjs --check && node --test tests/*.mjs",
    "sync:hooks": "node sync-hooks.mjs",
    "doctor": "node doctor.mjs",
    "install-hooks": "node install.mjs",
    "install-gh-shim": "node install-gh-shim.mjs",
    "agent:identity": "node agent-bot.mjs identity"
  }
}
