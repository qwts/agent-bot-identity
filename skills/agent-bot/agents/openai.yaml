interface:
  display_name: "Agent Bot"
  short_description: "Bootstrap and operate bot identities safely"
  default_prompt: "Use $agent-bot to bootstrap all configured agent identities from source or the installed CLI, operate linked-worktree identity, retrieve only explicitly authorized secrets, and publish with verified bot attribution."
